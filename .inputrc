# Disable console beeps that occur as alerts to gain operator attention.
set bell-style none

# If set to On, words with more than one possible completion are listed instead of ringing the bell (requires pressing Tab once, rather than twice).
set show-all-if-ambiguous on

# If set to On, readline does not insert characters from the completion that match characters after point in the word being completed, so portions of the word following the cursor are not duplicated. Mak[e]file !=> Makefilefile
set skip-completed-text on

# Search forward through bash history for a string of characters between the start of the current line and the cursor point.
"\e[B": history-search-forward

# Search backward through bash history for a string of characters between the start of the current line and the cursor point.
"\e[A": history-search-backward
