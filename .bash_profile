#!/usr/bin/env bash

#====================================================
# Bash Profile
#
# This script only needs to be sourced upon logging into a system to source the appropriate bashrc configuration file.
#====================================================

if [ -f ~/.bashrc ]; then
	source ~/.bashrc
fi
